# Re-installing littleboy

Boot into installer and generate facter.json
set up disko config
run disko script
luks password (lukspassword)
run nixos-install --flake (with lanzaboote disabled for first install)